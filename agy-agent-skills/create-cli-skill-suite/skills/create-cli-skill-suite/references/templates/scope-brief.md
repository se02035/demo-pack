# Scope brief (fill, then wait for accept)

Copy this into chat. Do not write product files until the user accepts.

**Goal** (1–3 sentences):

**In scope:**

**Out of scope (refuse / do not invent):**

**Ship-bar** (must work or become an issue id):

1.
2.

**Success example prompts:**

**Remote systems:**

**Read / mutate / long-running:**

**Auth mechanism** (names, not secrets):

**Target environment(s):**

**Constraints** (client, vendor CLI, docs/OpenAPI/proto):

**Existing vs create** (named production vs `agy-<slug>-` smokes):

**Suggested split** (preview only):

- Option A (split):
- Option B (single skill):

User: **accept / object / edits:**
